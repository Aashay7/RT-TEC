project: trade_eligibility
registry: data/registry.db
provider: local

online_store:
  type: redis
  connection_string: "redis:6379,db=0,password=${REDIS_PASSWORD:-redispassword}"

offline_store:
  type: file
entity_key_serialization_version: 2
